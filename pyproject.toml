[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "diffusion-sampling"
version = "0.1.0"
description = "Score matching, logSNR, eps-/v-reparametrization"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]